{
  "dependencies": {
    "razorpay": "^2.9.5",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.16.0"
  },
  "scripts": {
"client": "npm --prefix client start",        // For starting the client
    "client-build": "npm --prefix client run build", // For building the client
    "client-deploy": "npm --prefix client run deploy", // For deploying the client
    "server": "npm --prefix server start",        // For starting the server
    "server-build": "npm --prefix server run build", // For building the server
    "server-deploy": "npm --prefix server run deploy", // For deploying the server
    "deploy": "vercel --prod"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "gh-pages": "^6.3.0"
  },
  "homepage": "https://soumya-kolekar.github.io/react"
}
